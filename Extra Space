    public void maxCategory() {
    	float maximum = 0;
    	for(int i = 0; i < students.size(); i++) {
    		float temp = students.get(i).getDays().get(i).getCaffeine();
    		if(temp >= maximum) {
    			maximum = temp;
    		}
    	}
    	System.out.println(maximum);
    }
